import oneChoice
import multipleChoice

def selection(title,choices,current_active):
    oneChoice.selection(title,choices,current_active)

def multi_selection(title,choices):
        
    multipleChoice.multi_selection(title,choices)